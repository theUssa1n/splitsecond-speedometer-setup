#pragma once

// Embedded payloads. The setup carries everything it needs: the movie it has
// to install and the archive's own path list (used only to explain a miss).
#define IDR_DIALSINFO_GFX 101
#define IDR_ARK_PATHS     102
